package base_de_datos;

public class Bses {

	/*CREATE TABLE PACIENTE(
    DNI VARCHAR2(10) CONSTRAINT PACIENTE_DNI_PK PRIMARY KEY,
    NOMBRE VARCHAR2(155),
    APELLIDOS VARCHAR2(155),
    FECHA_NACIMIENTO DATE
);

CREATE TABLE ESPECIALIDAD(
    CODIGO NUMBER GENERATED BY DEFAULT AS IDENTITY CONSTRAINT ESPECIALIDAD_CODIGO_PK PRIMARY KEY, 
    NOMBRE VARCHAR2(155),
    SALARIO_BASE NUMBER (25)
);

CREATE TABLE MEDICO(
  NUM_COLEGIADO NUMBER GENERATED BY DEFAULT AS IDENTITY CONSTRAINT MEDICO_NUM_COLEGIADO_PK PRIMARY KEY,
    NOMBRE VARCHAR2(155),
    APELLIDO VARCHAR2(155),
    ESPECIALIDAD NUMBER CONSTRAINT MEDICO_ESPECIALIDAD_FK REFERENCES ESPECIALIDAD(CODIGO),
    TURNO_TRABAJO VARCHAR2(155),
    HACE_GUARDIAS NUMBER(1) DEFAULT 1
);

CREATE TABLE ASIGNADO(
    PACIENTE VARCHAR2(10) CONSTRAINT ASIGNADO_PACIENTE_FK REFERENCES PACIENTE(DNI) CONSTRAINT ASIGNADO_PACIENTE_PK PRIMARY KEY,
    MEDICO_CABECERA NUMBER CONSTRAINT ASIGNADO_MEDICO_CABECERA_FK REFERENCES MEDICO(NUM_COLEGIADO),
    FECHA_ASIGNACION DATE
);*/
DESC MEDICO;
--INSERT INTO PACIENTE VALUES('1001001Z', 'Manuel', 'Gil', DATE '2010-10-10');
--INSERT INTO PACIENTE VALUES('222222R', 'Darío', 'Herranz', DATE '2002-12-22');
SELECT * FROM PACIENTE;	
}
